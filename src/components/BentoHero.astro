---
import subjects from '../data/subjects.json';
import { getData } from '../lib/data.js';

let totalVideos = 0;
let totalChapters = 0;
for (const s of subjects) {
  const d = getData(s.id);
  totalChapters += d.chapters.length;
  for (const ch of d.chapters) {
    if (ch.videoCount) {
      totalVideos += ch.videoCount;
    } else if (ch.sections) {
      for (const sec of ch.sections) {
        totalVideos += (sec.videos || []).length;
      }
    } else if (ch.videos) {
      totalVideos += ch.videos.length;
    }
  }
}
const totalSubjects = subjects.length;
---

<div class="relative py-10 sm:py-16 text-center">
  <div class="relative">
    <!-- Badge -->
    <div class="anim inline-flex items-center gap-2 px-4 py-1.5 rounded-full mb-5 text-[13px] font-medium" style="
      background: rgba(45, 90, 123, 0.08);
      color: var(--accent);
      border: 1px solid rgba(45, 90, 123, 0.15);
    ">
      <span data-lang="heroBadge">Free Punjab Board Lectures — 2026 Syllabus</span>
    </div>

    <!-- Headline -->
    <h1 class="anim d1 text-[36px] sm:text-[48px] md:text-[56px] font-normal tracking-tight leading-[1.1] mb-4" style="font-family: var(--font-display); color: var(--text-primary);" data-lang="heroTitle">
      Learn smarter,<br/>not harder.
    </h1>

    <!-- Subtitle -->
    <p class="anim d2 text-[16px] sm:text-[18px] max-w-[520px] mx-auto leading-relaxed mb-10" style="color: var(--text-secondary);" data-lang="heroDesc">
      {totalVideos} video lectures across {totalSubjects} subjects. Chapter-wise, exercise-wise, completely free — built for Punjab Board 9th graders.
    </p>

    <!-- Stats row -->
    <div class="anim d3 flex justify-center gap-0 mb-10">
      <div class="text-center px-6 sm:px-9 relative">
        <div class="text-[28px] sm:text-[36px] font-semibold tracking-tight" style="font-family: var(--font-mono); color: var(--text-primary);">
          {totalSubjects}<span style="color: var(--accent);">+</span>
        </div>
        <div class="text-[13px] font-medium mt-0.5" style="color: var(--text-muted);" data-lang="subjects">Subjects</div>
      </div>
      <div class="text-center px-6 sm:px-9 relative" style="border-left: 1px solid var(--border);">
        <div class="text-[28px] sm:text-[36px] font-semibold tracking-tight" style="font-family: var(--font-mono); color: var(--text-primary);">{totalVideos}</div>
        <div class="text-[13px] font-medium mt-0.5" style="color: var(--text-muted);" data-lang="videos">Videos</div>
      </div>
      <div class="text-center px-6 sm:px-9 relative" style="border-left: 1px solid var(--border);">
        <div class="text-[28px] sm:text-[36px] font-semibold tracking-tight" style="font-family: var(--font-mono); color: var(--text-primary);">{totalChapters}</div>
        <div class="text-[13px] font-medium mt-0.5" style="color: var(--text-muted);">Chapters</div>
      </div>
      <div class="hidden sm:block text-center px-6 sm:px-9 relative" style="border-left: 1px solid var(--border);">
        <div class="text-[28px] sm:text-[36px] font-semibold tracking-tight" style="font-family: var(--font-mono); color: var(--text-primary);">∞</div>
        <div class="text-[13px] font-medium mt-0.5" style="color: var(--text-muted);">Free forever</div>
      </div>
    </div>

    <!-- Search bar -->
    <a href="/search/" class="anim d4 max-w-[520px] mx-auto relative block group" aria-label="Search lectures">
      <div class="relative flex items-center h-[54px] rounded-[14px] px-4 gap-3 transition-all duration-200" style="
        background: var(--surface);
        border: 1.5px solid var(--border);
        box-shadow: var(--shadow-card);
      ">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" style="color: var(--text-muted); flex-shrink: 0;">
          <circle cx="11" cy="11" r="8"/><path d="M21 21l-4.35-4.35"/>
        </svg>
        <span class="text-[15px]" style="color: var(--text-dim);" data-lang="searchPlaceholder">Search lectures... e.g. "Trigonometry Exercise 6.1"</span>
        <kbd class="hidden sm:flex ml-auto items-center gap-1 font-mono text-[11px] px-2 py-0.5 rounded" style="background: rgba(0,0,0,0.03); border: 1px solid var(--border); color: var(--text-muted);">/</kbd>
      </div>
    </a>
  </div>
</div>